<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ConsultorioApp.ViewModels"
             xmlns:local="clr-namespace:ConsultorioApp.Views"
             x:Class="ConsultorioApp.Views.PacientesPage"
             Title="Pacientes">

    <VerticalStackLayout Padding="20">
        <Label Text="Lista de Pacientes" 
               FontSize="20" 
               FontAttributes="Bold"
               HorizontalOptions="Center" />

        <!-- Botão para novo paciente -->
        <Button Text="Cadastrar Novo Paciente" 
                Command="{Binding NovoPacienteCommand}"
                Margin="0,10,0,20" />

        <!-- Lista de pacientes -->
        <CollectionView x:Name="PacientesList" ItemsSource="{Binding Pacientes}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BorderColor="LightGray" Padding="10" Margin="0,5">
                        <Grid ColumnDefinitions="*,Auto,Auto">
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="{Binding Nome}" FontSize="18" />
                                <Label Text="{Binding Telefone}" FontSize="14" TextColor="Gray"/>
                            </VerticalStackLayout>
                            
                            <Button Grid.Column="1"
                                    Text="Editar"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type local:PacientesPage}}, Path=BindingContext.EditarPacienteCommand}"
                                    CommandParameter="{Binding}"
                                    BackgroundColor="#2196F3"
                                    TextColor="White"
                                    CornerRadius="5"
                                    Padding="10,5"
                                    Margin="5,0" />
                            
                            <Button Grid.Column="2"
                                    Text="Excluir"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type local:PacientesPage}}, Path=BindingContext.ExcluirPacienteCommand}"
                                    CommandParameter="{Binding}"
                                    BackgroundColor="#F44336"
                                    TextColor="White"
                                    CornerRadius="5"
                                    Padding="10,5" />
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>
